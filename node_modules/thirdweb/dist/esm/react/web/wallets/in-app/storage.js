import { getStorage } from "../../../core/storage.js";
const LAST_AUTH_PROVIDER_STORAGE_KEY = "lastAuthProvider";
export async function setLastAuthProvider(authProvider) {
    await getStorage().setItem(LAST_AUTH_PROVIDER_STORAGE_KEY, authProvider);
}
export async function getLastAuthProvider(storage) {
    return (await storage.getItem(LAST_AUTH_PROVIDER_STORAGE_KEY));
}
//# sourceMappingURL=storage.js.map