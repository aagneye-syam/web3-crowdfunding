"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.incrementNonce = exports.encodeIncrementNonce = exports.encodeIncrementNonceParams = exports.isIncrementNonceSupported = exports.FN_SELECTOR = void 0;
const prepare_contract_call_js_1 = require("../../../../../transaction/prepare-contract-call.js");
const encodeAbiParameters_js_1 = require("../../../../../utils/abi/encodeAbiParameters.js");
const once_js_1 = require("../../../../../utils/promise/once.js");
const detectExtension_js_1 = require("../../../../../utils/bytecode/detectExtension.js");
exports.FN_SELECTOR = "0x0bd28e3b";
const FN_INPUTS = [
    {
        type: "uint192",
        name: "key",
    },
];
const FN_OUTPUTS = [];
/**
 * Checks if the `incrementNonce` method is supported by the given contract.
 * @param contract The ThirdwebContract.
 * @returns A promise that resolves to a boolean indicating if the `incrementNonce` method is supported.
 * @extension ERC4337
 * @example
 * ```ts
 * import { isIncrementNonceSupported } from "thirdweb/extensions/erc4337";
 *
 * const supported = await isIncrementNonceSupported(contract);
 * ```
 */
async function isIncrementNonceSupported(contract) {
    return (0, detectExtension_js_1.detectMethod)({
        contract,
        method: [exports.FN_SELECTOR, FN_INPUTS, FN_OUTPUTS],
    });
}
exports.isIncrementNonceSupported = isIncrementNonceSupported;
/**
 * Encodes the parameters for the "incrementNonce" function.
 * @param options - The options for the incrementNonce function.
 * @returns The encoded ABI parameters.
 * @extension ERC4337
 * @example
 * ```ts
 * import { encodeIncrementNonceParams } "thirdweb/extensions/erc4337";
 * const result = encodeIncrementNonceParams({
 *  key: ...,
 * });
 * ```
 */
function encodeIncrementNonceParams(options) {
    return (0, encodeAbiParameters_js_1.encodeAbiParameters)(FN_INPUTS, [options.key]);
}
exports.encodeIncrementNonceParams = encodeIncrementNonceParams;
/**
 * Encodes the "incrementNonce" function into a Hex string with its parameters.
 * @param options - The options for the incrementNonce function.
 * @returns The encoded hexadecimal string.
 * @extension ERC4337
 * @example
 * ```ts
 * import { encodeIncrementNonce } "thirdweb/extensions/erc4337";
 * const result = encodeIncrementNonce({
 *  key: ...,
 * });
 * ```
 */
function encodeIncrementNonce(options) {
    // we do a "manual" concat here to avoid the overhead of the "concatHex" function
    // we can do this because we know the specific formats of the values
    return (exports.FN_SELECTOR +
        encodeIncrementNonceParams(options).slice(2));
}
exports.encodeIncrementNonce = encodeIncrementNonce;
/**
 * Prepares a transaction to call the "incrementNonce" function on the contract.
 * @param options - The options for the "incrementNonce" function.
 * @returns A prepared transaction object.
 * @extension ERC4337
 * @example
 * ```ts
 * import { incrementNonce } from "thirdweb/extensions/erc4337";
 *
 * const transaction = incrementNonce({
 *  contract,
 *  key: ...,
 *  overrides: {
 *    ...
 *  }
 * });
 *
 * // Send the transaction
 * ...
 *
 * ```
 */
function incrementNonce(options) {
    const asyncOptions = (0, once_js_1.once)(async () => {
        return "asyncParams" in options ? await options.asyncParams() : options;
    });
    return (0, prepare_contract_call_js_1.prepareContractCall)({
        contract: options.contract,
        method: [exports.FN_SELECTOR, FN_INPUTS, FN_OUTPUTS],
        params: async () => {
            const resolvedOptions = await asyncOptions();
            return [resolvedOptions.key];
        },
        value: async () => (await asyncOptions()).overrides?.value,
        accessList: async () => (await asyncOptions()).overrides?.accessList,
        gas: async () => (await asyncOptions()).overrides?.gas,
        gasPrice: async () => (await asyncOptions()).overrides?.gasPrice,
        maxFeePerGas: async () => (await asyncOptions()).overrides?.maxFeePerGas,
        maxPriorityFeePerGas: async () => (await asyncOptions()).overrides?.maxPriorityFeePerGas,
        nonce: async () => (await asyncOptions()).overrides?.nonce,
        extraGas: async () => (await asyncOptions()).overrides?.extraGas,
    });
}
exports.incrementNonce = incrementNonce;
//# sourceMappingURL=incrementNonce.js.map