"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.reverse = exports.decodeReverseResult = exports.encodeReverse = exports.encodeReverseParams = exports.isReverseSupported = exports.FN_SELECTOR = void 0;
const read_contract_js_1 = require("../../../../../transaction/read-contract.js");
const encodeAbiParameters_js_1 = require("../../../../../utils/abi/encodeAbiParameters.js");
const viem_1 = require("viem");
const detectExtension_js_1 = require("../../../../../utils/bytecode/detectExtension.js");
exports.FN_SELECTOR = "0xec11c823";
const FN_INPUTS = [
    {
        type: "bytes",
        name: "reverseName",
    },
];
const FN_OUTPUTS = [
    {
        type: "string",
    },
    {
        type: "address",
    },
    {
        type: "address",
    },
    {
        type: "address",
    },
];
/**
 * Checks if the `reverse` method is supported by the given contract.
 * @param contract The ThirdwebContract.
 * @returns A promise that resolves to a boolean indicating if the `reverse` method is supported.
 * @extension ENS
 * @example
 * ```ts
 * import { isReverseSupported } from "thirdweb/extensions/ens";
 *
 * const supported = await isReverseSupported(contract);
 * ```
 */
async function isReverseSupported(contract) {
    return (0, detectExtension_js_1.detectMethod)({
        contract,
        method: [exports.FN_SELECTOR, FN_INPUTS, FN_OUTPUTS],
    });
}
exports.isReverseSupported = isReverseSupported;
/**
 * Encodes the parameters for the "reverse" function.
 * @param options - The options for the reverse function.
 * @returns The encoded ABI parameters.
 * @extension ENS
 * @example
 * ```ts
 * import { encodeReverseParams } "thirdweb/extensions/ens";
 * const result = encodeReverseParams({
 *  reverseName: ...,
 * });
 * ```
 */
function encodeReverseParams(options) {
    return (0, encodeAbiParameters_js_1.encodeAbiParameters)(FN_INPUTS, [options.reverseName]);
}
exports.encodeReverseParams = encodeReverseParams;
/**
 * Encodes the "reverse" function into a Hex string with its parameters.
 * @param options - The options for the reverse function.
 * @returns The encoded hexadecimal string.
 * @extension ENS
 * @example
 * ```ts
 * import { encodeReverse } "thirdweb/extensions/ens";
 * const result = encodeReverse({
 *  reverseName: ...,
 * });
 * ```
 */
function encodeReverse(options) {
    // we do a "manual" concat here to avoid the overhead of the "concatHex" function
    // we can do this because we know the specific formats of the values
    return (exports.FN_SELECTOR +
        encodeReverseParams(options).slice(2));
}
exports.encodeReverse = encodeReverse;
/**
 * Decodes the result of the reverse function call.
 * @param result - The hexadecimal result to decode.
 * @returns The decoded result as per the FN_OUTPUTS definition.
 * @extension ENS
 * @example
 * ```ts
 * import { decodeReverseResult } from "thirdweb/extensions/ens";
 * const result = decodeReverseResult("...");
 * ```
 */
function decodeReverseResult(result) {
    return (0, viem_1.decodeAbiParameters)(FN_OUTPUTS, result);
}
exports.decodeReverseResult = decodeReverseResult;
/**
 * Calls the "reverse" function on the contract.
 * @param options - The options for the reverse function.
 * @returns The parsed result of the function call.
 * @extension ENS
 * @example
 * ```ts
 * import { reverse } from "thirdweb/extensions/ens";
 *
 * const result = await reverse({
 *  contract,
 *  reverseName: ...,
 * });
 *
 * ```
 */
async function reverse(options) {
    return (0, read_contract_js_1.readContract)({
        contract: options.contract,
        method: [exports.FN_SELECTOR, FN_INPUTS, FN_OUTPUTS],
        params: [options.reverseName],
    });
}
exports.reverse = reverse;
//# sourceMappingURL=reverse.js.map