"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.pubkey = exports.decodePubkeyResult = exports.encodePubkey = exports.encodePubkeyParams = exports.isPubkeySupported = exports.FN_SELECTOR = void 0;
const read_contract_js_1 = require("../../../../../transaction/read-contract.js");
const encodeAbiParameters_js_1 = require("../../../../../utils/abi/encodeAbiParameters.js");
const viem_1 = require("viem");
const detectExtension_js_1 = require("../../../../../utils/bytecode/detectExtension.js");
exports.FN_SELECTOR = "0xc8690233";
const FN_INPUTS = [
    {
        type: "bytes32",
        name: "name",
    },
];
const FN_OUTPUTS = [
    {
        type: "bytes32",
        name: "x",
    },
    {
        type: "bytes32",
        name: "y",
    },
];
/**
 * Checks if the `pubkey` method is supported by the given contract.
 * @param contract The ThirdwebContract.
 * @returns A promise that resolves to a boolean indicating if the `pubkey` method is supported.
 * @extension ENS
 * @example
 * ```ts
 * import { isPubkeySupported } from "thirdweb/extensions/ens";
 *
 * const supported = await isPubkeySupported(contract);
 * ```
 */
async function isPubkeySupported(contract) {
    return (0, detectExtension_js_1.detectMethod)({
        contract,
        method: [exports.FN_SELECTOR, FN_INPUTS, FN_OUTPUTS],
    });
}
exports.isPubkeySupported = isPubkeySupported;
/**
 * Encodes the parameters for the "pubkey" function.
 * @param options - The options for the pubkey function.
 * @returns The encoded ABI parameters.
 * @extension ENS
 * @example
 * ```ts
 * import { encodePubkeyParams } "thirdweb/extensions/ens";
 * const result = encodePubkeyParams({
 *  name: ...,
 * });
 * ```
 */
function encodePubkeyParams(options) {
    return (0, encodeAbiParameters_js_1.encodeAbiParameters)(FN_INPUTS, [options.name]);
}
exports.encodePubkeyParams = encodePubkeyParams;
/**
 * Encodes the "pubkey" function into a Hex string with its parameters.
 * @param options - The options for the pubkey function.
 * @returns The encoded hexadecimal string.
 * @extension ENS
 * @example
 * ```ts
 * import { encodePubkey } "thirdweb/extensions/ens";
 * const result = encodePubkey({
 *  name: ...,
 * });
 * ```
 */
function encodePubkey(options) {
    // we do a "manual" concat here to avoid the overhead of the "concatHex" function
    // we can do this because we know the specific formats of the values
    return (exports.FN_SELECTOR +
        encodePubkeyParams(options).slice(2));
}
exports.encodePubkey = encodePubkey;
/**
 * Decodes the result of the pubkey function call.
 * @param result - The hexadecimal result to decode.
 * @returns The decoded result as per the FN_OUTPUTS definition.
 * @extension ENS
 * @example
 * ```ts
 * import { decodePubkeyResult } from "thirdweb/extensions/ens";
 * const result = decodePubkeyResult("...");
 * ```
 */
function decodePubkeyResult(result) {
    return (0, viem_1.decodeAbiParameters)(FN_OUTPUTS, result);
}
exports.decodePubkeyResult = decodePubkeyResult;
/**
 * Calls the "pubkey" function on the contract.
 * @param options - The options for the pubkey function.
 * @returns The parsed result of the function call.
 * @extension ENS
 * @example
 * ```ts
 * import { pubkey } from "thirdweb/extensions/ens";
 *
 * const result = await pubkey({
 *  contract,
 *  name: ...,
 * });
 *
 * ```
 */
async function pubkey(options) {
    return (0, read_contract_js_1.readContract)({
        contract: options.contract,
        method: [exports.FN_SELECTOR, FN_INPUTS, FN_OUTPUTS],
        params: [options.name],
    });
}
exports.pubkey = pubkey;
//# sourceMappingURL=pubkey.js.map