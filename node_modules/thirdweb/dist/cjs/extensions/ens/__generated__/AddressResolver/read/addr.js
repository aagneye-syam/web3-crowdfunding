"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.addr = exports.decodeAddrResult = exports.encodeAddr = exports.encodeAddrParams = exports.isAddrSupported = exports.FN_SELECTOR = void 0;
const read_contract_js_1 = require("../../../../../transaction/read-contract.js");
const encodeAbiParameters_js_1 = require("../../../../../utils/abi/encodeAbiParameters.js");
const viem_1 = require("viem");
const detectExtension_js_1 = require("../../../../../utils/bytecode/detectExtension.js");
exports.FN_SELECTOR = "0x3b3b57de";
const FN_INPUTS = [
    {
        type: "bytes32",
        name: "name",
    },
];
const FN_OUTPUTS = [
    {
        type: "address",
    },
];
/**
 * Checks if the `addr` method is supported by the given contract.
 * @param contract The ThirdwebContract.
 * @returns A promise that resolves to a boolean indicating if the `addr` method is supported.
 * @extension ENS
 * @example
 * ```ts
 * import { isAddrSupported } from "thirdweb/extensions/ens";
 *
 * const supported = await isAddrSupported(contract);
 * ```
 */
async function isAddrSupported(contract) {
    return (0, detectExtension_js_1.detectMethod)({
        contract,
        method: [exports.FN_SELECTOR, FN_INPUTS, FN_OUTPUTS],
    });
}
exports.isAddrSupported = isAddrSupported;
/**
 * Encodes the parameters for the "addr" function.
 * @param options - The options for the addr function.
 * @returns The encoded ABI parameters.
 * @extension ENS
 * @example
 * ```ts
 * import { encodeAddrParams } "thirdweb/extensions/ens";
 * const result = encodeAddrParams({
 *  name: ...,
 * });
 * ```
 */
function encodeAddrParams(options) {
    return (0, encodeAbiParameters_js_1.encodeAbiParameters)(FN_INPUTS, [options.name]);
}
exports.encodeAddrParams = encodeAddrParams;
/**
 * Encodes the "addr" function into a Hex string with its parameters.
 * @param options - The options for the addr function.
 * @returns The encoded hexadecimal string.
 * @extension ENS
 * @example
 * ```ts
 * import { encodeAddr } "thirdweb/extensions/ens";
 * const result = encodeAddr({
 *  name: ...,
 * });
 * ```
 */
function encodeAddr(options) {
    // we do a "manual" concat here to avoid the overhead of the "concatHex" function
    // we can do this because we know the specific formats of the values
    return (exports.FN_SELECTOR +
        encodeAddrParams(options).slice(2));
}
exports.encodeAddr = encodeAddr;
/**
 * Decodes the result of the addr function call.
 * @param result - The hexadecimal result to decode.
 * @returns The decoded result as per the FN_OUTPUTS definition.
 * @extension ENS
 * @example
 * ```ts
 * import { decodeAddrResult } from "thirdweb/extensions/ens";
 * const result = decodeAddrResult("...");
 * ```
 */
function decodeAddrResult(result) {
    return (0, viem_1.decodeAbiParameters)(FN_OUTPUTS, result)[0];
}
exports.decodeAddrResult = decodeAddrResult;
/**
 * Calls the "addr" function on the contract.
 * @param options - The options for the addr function.
 * @returns The parsed result of the function call.
 * @extension ENS
 * @example
 * ```ts
 * import { addr } from "thirdweb/extensions/ens";
 *
 * const result = await addr({
 *  contract,
 *  name: ...,
 * });
 *
 * ```
 */
async function addr(options) {
    return (0, read_contract_js_1.readContract)({
        contract: options.contract,
        method: [exports.FN_SELECTOR, FN_INPUTS, FN_OUTPUTS],
        params: [options.name],
    });
}
exports.addr = addr;
//# sourceMappingURL=addr.js.map