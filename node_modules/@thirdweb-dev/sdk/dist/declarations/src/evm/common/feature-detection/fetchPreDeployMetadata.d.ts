import { PreDeployMetadataFetched } from "../../schema/contracts/custom";
import { ThirdwebStorage } from "@thirdweb-dev/storage";
import { CompilerOptions } from "../../types/compiler/compiler-options";
/**
 * Fetch the metadata coming from CLI, this is before deploying or releasing the contract.
 * @internal
 * @param publishMetadataUri - The publish metadata URI to fetch
 * @param storage - The storage to use
 */
export declare function fetchPreDeployMetadata(publishMetadataUri: string, storage: ThirdwebStorage, compilerOptions?: CompilerOptions): Promise<PreDeployMetadataFetched>;
//# sourceMappingURL=fetchPreDeployMetadata.d.ts.map